import EmptyState from "../ui/EmptyState";
export default function ProjectJobsPanel({ jobs, onOpenJob }) { return <article className="dashboard-card"><h3>Project jobs</h3>{jobs.length?jobs.map(job=><button className="job-row" key={job.job_id} onClick={()=>onOpenJob(job.job_id)}><span>{job.type}</span><span className={`status-badge ${job.status}`}>{job.status}</span><small>{job.progress_current}/{job.progress_total} · {new Date(job.created_at).toLocaleString()}</small>{job.error?<em className="job-error">{job.error.message}</em>:null}</button>):<EmptyState title="No project jobs yet" description="Queue a generator as a project job and it will appear here."/>}</article>; }
