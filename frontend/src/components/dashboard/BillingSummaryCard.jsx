const usd=value=>`$${(value/100).toFixed(2)}`;
import WarningCallout from "../ui/WarningCallout";
export default function BillingSummaryCard({ billing }) { const warning=billing?.available_credits<=0?"This project has no available credits. Top up before running paid jobs.":billing?.available_credits<=100?"This project is running low on credits.":null;return <article className="dashboard-card"><h3>Internal credits</h3>{billing?<><strong className="metric">{billing.available_credits}</strong><p>Available · {usd(billing.available_credits)} internal value</p><small>{billing.balance_credits} balance · {billing.reserved_credits} reserved · {billing.lifetime_used_credits} used</small>{warning?<WarningCallout>{warning}</WarningCallout>:null}</>:null}</article>; }
