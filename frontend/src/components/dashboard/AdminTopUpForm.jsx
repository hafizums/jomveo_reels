export default function AdminTopUpForm({ amount, onAmountChange, onSubmit, submitting=false }) { return <form className="inline-form" onSubmit={onSubmit}><input aria-label="Top-up amount" type="number" min="1" value={amount} onChange={event=>onAmountChange(event.target.value)} disabled={submitting}/><button disabled={submitting}>{submitting?"Topping up…":"Admin top-up"}</button></form>; }
